server:
  port: 8088

spring:
  application:
    name: ms-cliente
  datasource:
    url: jdbc:mysql://localhost:3306/db_clientes?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true