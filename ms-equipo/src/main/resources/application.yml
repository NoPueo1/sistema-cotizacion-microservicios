server:
  port: 8090

spring:
  application:
    name: ms-equipo
  datasource:
    url: jdbc:mysql://localhost:3306/db_equipos?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

categoria:
  service:
    url: localhost:8087